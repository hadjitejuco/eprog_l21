//arrays
#include <stdio.h>
int main(){
	int a[100];
	int i, n;
	printf ("Enter n: ");
	scanf ("%d",&n);
	printf ("\nEnter %d numbers\n",n);
	for (i=0;i<n;i++){
		scanf ("%d",&a[i]);
	}
	printf ("\nEntered numbers are \n");
	for (i=0; i<n;i++){
		printf ("a[%d] - %d\n",i,a[i]);
	}
	return 0;
}
